'use client';

import { PropsWithChildren } from 'react';

const DebugAtoms = () => {
  // useAtomsDebugValue();
  return null;
};

export default function RootProvider({ children }: PropsWithChildren) {
  return children;
}
